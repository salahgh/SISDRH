

query allUsersPaged($pageable : PaginationInput){
    allUsersPaged(pageable: $pageable){
        content {
            id ,
            personnel {
                matricule,
                noma,
                pnoma
                nom
                pnom
            }
        }
        totalElements
    }
}
